# Analytics Module

## Overview
Lightweight analytics processing module.

## Structure
- `index.ts`: Main entry point
- `app.py`: Python analytics server

## Removed Features
- 4D Flow Visualization (heavy WebGPU rendering)
- WebGPU renderer module

For basic analytics operations, use `index.ts` or `app.py`.
